# Helldivers 2 随机器设计方案

## 1. 项目定位

这是一个面向本地和朋友使用的 Helldivers 2 网页随机器。目标不是做完整资料站，而是快速生成可玩的随机结果，减少开局前讨论配装的时间。

第一版以本地网页工具为主，不接账号系统、不接云端数据库、不考虑商业发布。数据和图标都随项目本地保存，打开网页即可使用。

## 2. 核心需求

第一版需要支持以下随机内容：

- 随机敌方阵营：终结族、光能族、机器人。
- 随机战备：从已有战备池中随机抽取 4 个。
- 随机主武器、副武器、手雷。
- 多人自定义战备池：2-4 名玩家各自构建多组 4 战备组合，加入公共随机池，然后每名玩家从池中抽取一组，且抽取结果不重复。
- 图标展示：使用已经从 Helldivers Wiki 下载到本地的图标资源。

## 3. 推荐技术方案

使用纯前端网页实现：

- Vite
- React
- TypeScript
- CSS Modules 或普通 CSS
- 本地 JSON/TS 数据文件
- localStorage 保存用户偏好和自定义战备池

不建议第一版使用后端数据库。当前需求主要是数据筛选和随机抽取，纯前端足够稳定，也方便发给朋友使用。

如果之后需要多人实时同步、账号、历史记录排行榜，再考虑 Supabase 或其他后端服务。

## 4. 项目结构建议

```text
RandomHD2/
  需求.md
  设计.md
  assets/
    wiki/
      README.md
      manifest.json
      factions/
      stratagems/
      weapons/
  public/
    assets/
      wiki/
  src/
    app/
      App.tsx
    data/
      factions.ts
      stratagems.ts
      weapons.ts
      grenades.ts
    features/
      quick-random/
      squad-pool/
      data-browser/
    lib/
      random.ts
      assetManifest.ts
      storage.ts
    types/
      loadout.ts
```

说明：

- `assets/wiki` 保存原始下载素材和来源记录。
- `public/assets/wiki` 用于前端直接访问的静态资源。实现时可以复制或移动现有素材。
- `src/data` 保存整理后的游戏数据，不直接把随机逻辑写进数据文件。
- `src/lib/random.ts` 保存纯随机函数，方便测试。
- `src/features` 按功能区拆分界面。

## 5. 数据模型

### 5.1 阵营

```ts
type Faction = {
  id: "terminids" | "illuminate" | "automatons";
  nameZh: string;
  nameEn: string;
  icon: string;
};
```

中文名建议：

- `terminids`: 终结族
- `illuminate`: 光能族
- `automatons`: 机器人

如果后续想和游戏官方中文翻译完全一致，只需要改 `nameZh`。

### 5.2 战备

```ts
type Stratagem = {
  id: string;
  nameZh?: string;
  nameEn: string;
  category: "patriotic" | "orbital" | "eagle" | "bridge" | "engineering" | "robotics" | "warbond" | "mission";
  icon: string;
  enabled: boolean;
  tags?: string[];
};
```

第一版可以先使用英文名和图标。中文名可以后续逐步补充，不影响随机功能。

### 5.3 武器和手雷

```ts
type Weapon = {
  id: string;
  nameZh?: string;
  nameEn: string;
  slot: "primary" | "secondary" | "grenade";
  category: string;
  icon: string;
  enabled: boolean;
};
```

从 Wiki 下载的 `weapons` 目录里包含主武器、副武器、支援武器、手雷、穿甲图标。整理数据时只把第一版需要的 `primary`、`secondary`、`grenade` 纳入随机池。

### 5.4 多人战备组合

```ts
type StratagemSet = {
  id: string;
  ownerName: string;
  name: string;
  stratagemIds: [string, string, string, string];
};

type SquadDrawResult = {
  playerName: string;
  set: StratagemSet;
};
```

抽取规则：

- 玩家人数限制为 2-4。
- 每组战备必须刚好 4 个。
- 公共池可包含所有玩家创建的组合。
- 抽取时每名玩家拿到 1 组。
- 同一轮抽取中组合不重复。
- 如果公共池数量少于玩家数，提示用户补充组合。

## 6. 图标和素材策略

当前已从 Helldivers Wiki 下载素材到：

```text
assets/wiki/
```

已下载内容：

- `factions`: 3 个阵营图标
- `stratagems`: 104 个战备图标
- `weapons`: 134 个武器、手雷、支援武器、穿甲相关图标
- `manifest.json`: 每个素材的本地路径、来源 URL、尺寸、类型、别名
- `README.md`: 素材来源和版权提醒

第一版实现时建议：

- 前端只引用本地文件，不热链 Wiki。
- 数据条目中的 `icon` 字段指向本地资源路径。
- 如果某个条目没有图标，显示默认分类图标。
- 保留 `manifest.json`，方便之后追踪来源和更新素材。

因为当前用途是本地和朋友使用，可以接受使用 Wiki 素材。若未来公开发布，应重新评估版权，或改成自制抽象图标。

## 7. 页面设计

第一版建议做成单页工具，不做营销首页。

### 7.1 快速随机区

用途：一键生成一套完整娱乐配装。

展示内容：

- 敌方阵营
- 4 个战备
- 主武器
- 副武器
- 手雷

操作：

- `全部随机`
- `重抽阵营`
- `重抽战备`
- `重抽主武器`
- `重抽副武器`
- `重抽手雷`

### 7.2 多人战备池区

用途：朋友开黑时，每个人把自己想玩的战备组合放入池子，再统一随机分配。

界面元素：

- 玩家人数选择：2、3、4
- 玩家名称输入
- 战备组合创建器
- 公共组合池列表
- 一键抽取
- 抽取结果卡片

组合创建器应支持：

- 从战备列表中选择 4 个。
- 给组合命名，例如“全轨道快乐组”、“机枪防守组”。
- 标记创建者。
- 删除或编辑组合。

### 7.3 数据浏览和筛选区

用途：让用户知道随机池里有哪些东西，并允许临时禁用不想玩的内容。

第一版可以做成三个标签页：

- 战备
- 主/副武器
- 手雷

每个条目显示：

- 图标
- 名称
- 类型
- 是否启用

筛选条件：

- 分类
- 关键词搜索
- 仅显示启用项

## 8. 随机逻辑

随机逻辑放在纯函数中，避免和 UI 绑定。

核心函数：

```ts
pickOne<T>(items: T[]): T
pickManyUnique<T>(items: T[], count: number): T[]
rollQuickLoadout(config): QuickLoadout
drawSquadSets(players, pool): SquadDrawResult[]
```

错误处理：

- 如果随机池为空，返回明确错误。
- 如果战备数量少于 4，提示无法生成 4 战备。
- 如果多人组合池数量少于玩家数，提示需要更多组合。
- 如果禁用项导致可选内容不足，提示用户恢复部分条目。

随机算法不需要复杂权重。第一版使用等概率随机即可。后续可以增加：

- 收藏项权重
- 排除上一次结果
- 按分类平衡战备
- 高难度/娱乐向预设

## 9. 本地存储

使用 `localStorage` 保存：

- 启用/禁用状态
- 玩家名称
- 自定义战备组合池
- 最近一次随机结果

建议 key：

```text
randomhd2.enabledItems
randomhd2.players
randomhd2.stratagemSets
randomhd2.lastRoll
```

需要提供“重置本地数据”按钮，方便清空错误配置。

## 10. 第一版交付范围

第一版只做必要功能：

- 初始化 React + TypeScript 网页项目。
- 整理本地素材到前端可访问目录。
- 建立基础数据文件。
- 实现快速随机。
- 实现多人战备池抽取。
- 实现本地保存。
- 实现图标展示和缺图 fallback。

暂不做：

- 账号系统
- 云同步
- 排行榜
- 实时联机协作
- 完整中文翻译
- 自动从 Wiki 更新数据
- 商业发布适配

## 11. 后续扩展

可以按优先级逐步增加：

1. 数据导入导出：把玩家组合池导出为 JSON，发给朋友导入。
2. 中文名补全：逐步维护中文译名。
3. 预设模式：娱乐、硬核、全随机、禁用重复分类等。
4. Wiki 更新脚本：重新抓取素材和数据，生成差异报告。
5. 桌面版：用 Tauri 或 Electron 包装网页。
6. 公开版素材替换：改用自制图标，降低版权风险。

## 12. 实现注意事项

- 数据文件和随机逻辑必须分开，方便游戏更新时只改数据。
- 图标路径必须统一由工具函数解析，不要在组件里拼接复杂路径。
- 多人抽取必须保证同一轮不重复。
- UI 上要清楚显示当前随机池是否足够。
- 本地存储读取失败时要能回退到默认状态。
- 大图标资源较多，页面应避免一次性渲染过多图片；数据浏览区可以分页或懒加载。
- 由于项目用于本地和朋友使用，版权风险较低，但仍应保留素材来源说明。

## 13. 推荐实现顺序

1. 搭建 Vite + React + TypeScript 项目。
2. 把 `assets/wiki` 中需要前端访问的资源复制到 `public/assets/wiki`。
3. 根据 `manifest.json` 整理第一版 `factions.ts`、`stratagems.ts`、`weapons.ts`。
4. 编写 `random.ts` 并用简单单元测试验证抽取逻辑。
5. 实现快速随机页面。
6. 实现多人战备池页面。
7. 接入 localStorage。
8. 做一次本地浏览器验证，检查图标、随机结果和多人抽取边界情况。

## 14. 设计结论

本项目第一版最适合做成纯前端本地网页工具。数据用本地 TS/JSON 文件维护，图标使用已经下载的 Wiki 本地素材，用户配置用 localStorage 保存。这样开发成本低、使用门槛低、方便发给朋友，也不会被后端、账号和部署流程拖慢。

后续如果使用频率高，再考虑做数据更新脚本、导入导出、桌面版或公开发布版。

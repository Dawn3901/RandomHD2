# RandomHD2 使用说明

## 单人本地调试

```powershell
npm run dev
```

打开：

```text
http://127.0.0.1:5173/
```

这个模式主要用于自己开发和预览，不提供多人同步。

## 和朋友同步使用

```powershell
npm run share
```

这个命令会先构建网页，然后启动本地同步服务。网页和 WebSocket 同时走同一个地址：

```text
http://127.0.0.1:5173/
```

如果用 SakuraFRP，把 HTTP 隧道目标设置为：

```text
127.0.0.1:5173
```

然后把 SakuraFRP 给出的公网地址发给朋友。你和朋友打开同一个地址后，以下内容会同步：

- 玩家列表
- 自定义战备组合池
- 多人战备池抽取结果
- 历史配装

快速随机和启用/禁用随机池仍然是各自浏览器本地状态。

作为服务器启动时，共享状态会保存在本机项目目录：

```text
.randomhd2/sync-state.json
```

下次继续用这台电脑运行 `npm run share` 时，之前创建过的历史配装会自动恢复。这个文件只保存在本地，不会提交到 git。

## 端口被占用

如果 `5173` 被之前的开发服务占用，可以先关掉旧服务，或改用其他端口：

```powershell
npm run build
npm run sync -- --host 0.0.0.0 --port 5174
```

SakuraFRP 目标也要对应改成：

```text
127.0.0.1:5174
```
